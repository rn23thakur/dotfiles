# 1. Create the configuration directory on the Mac
mkdir -p ~/.config/wezterm

# 2. Copy your config file into it named exactly 'wezterm.lua'
cp ~/dotfiles/wezterm/wezterm.lua ~/.config/wezterm/wezterm.lua
